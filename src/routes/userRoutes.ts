import express from 'express';


const router = express.Router();

//TODO :: Finish user routes - needed user model?
router.get('/' , )
router.get('/:id',)
router.post('/',)
router.put('/:id',)
router.delete('/:id',)


export default router;